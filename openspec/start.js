#!/usr/bin/env node

// Astro's entry.mjs auto-starts the server, so we just need to import it
// The server will start automatically on the configured port
import './dist/server/entry.mjs';

console.log('OpenSpec server starting...');
